# PUAX 演进总纲 · v4.3 → v5.0

> 审阅日期：2026-09-17 ｜ 审阅范围：全仓（源码 / 评测 / 角色 / 分发 / 文档 / CI）
> 当前版本：**4.3.1**（puax-mcp-server）｜ 角色 59 ｜ 工具 50 ｜ 守门 32 门 ｜ Jest 1151 例（91 套件）
>
> **行军进度（2026-09-17 夜 · 首战：P0 攻坚；二战：v4.5 通脉；三战：审查收口与会话加固）**
>
> | 项 | 状态 | 落点 |
> |----|:---:|------|
> | **P0-1** 工具 schema 契约 | ✅ | 新增 `src/tools/json-schema.ts`（零依赖 Zod→JSON Schema）；`server/core.ts` 出口经 `toProtocolTool()` 编译 + `callTool` 补 `safeParse` 执行侧；`test/core/json-schema.test.ts` 13 例守门 |
> | **P0-2** HTTP 面收口 | ✅ | `server/core.ts` 增 `loopbackOrigin()`，CORS 仅环回来源授权（原 `*` 全开）；`server/v4-http.ts` 的 `/v4/doctor/fix` 必须要求 `allow_write: true`，未授权即 403；`test/core/host-doctor.test.ts` 全面沙箱隔离 |
> | **P0-3** 幽灵端点 | ✅ | 启动横幅删去不存在的 `/message`，`/` 与 `/mcp` 合并说明为 Streamable HTTP（eventStore 续传仍待 v4.5） |
> | **P0-4** 防作弊失败即放行 | ✅ | `cli/hook-cli.ts` 兜底 catch：PreToolUse + claude 宿主改为保守拒绝（`PUAX_GUARD_ERROR`），其余仍优雅降级 |
> | **P0-5** 护栏可绕过 | ✅ | `core/anti-cheat-guard.ts` 增 `normalizePathForMatch()` / `normalizeCommandForMatch()`，原串与归一化视图**双候选任一命中即拦**；`test/core/anti-cheat-normalize.test.ts` 8 例 |
> | **P0-6** 包导出面 | ✅ | `package.json` 增 `exports`：`.` / `./amp` / `./amp-spec` / `./thin-prompt` / `./hook-cli` 五入口 + `./build/*` 通配 |
> | **4.5.1** TTF 墙钟化 | ✅ | `src/core/ttf.ts` 增 `wall_clock_ms`（自 `performance.timeOrigin` 起算）与 `median_wall_clock_ms`；`evals/test-ttf.js` 改测子进程真实冷启动首压（~103ms 墙钟），双口径公开 |
> | **4.5.2** 删固定 200ms | ✅ | `cli/hook-cli.ts` 的 `readStdinPayload` 遇 TTY 立解，非 TTY 降至 20ms 超时并加 `timer.unref()`，消除事件循环残留阻滞 |
> | **4.5.3** 角色资产懒加载 | ✅ | `generate-bundle.js` 将 `SKILL_MANIFEST` 导出置于 Proxy 懒加载，`getBundledSkillById` 由 ID 前缀直接锁定单一类别 bundle，hook/TTF 冷启动解析量自 164KB 压降 >85%（实测仅载入 15KB 单 bundle） |
> | **4.5.4** 多会话 Streamable HTTP | ✅ | `server/core.ts` 打造 `createMcpServerInstance()` 工厂，多客户端会话分配独立实例与 Session ID；支持 DELETE 会话注销与防重入爆栈守卫；`test/integration/http-multi-session.test.ts` 固化集成守门 |
> | **4.5.5** 宿主探测未知降级 | ✅ | `cli/hook-cli.ts` 的 `detectHarness()` 遇未知平台显式返回 `'unknown'` 并发出警告，平稳降级输出通用 `additionalContext` |
> | **4.5.6** 补齐 MCP 现代特性 | ✅ | `server/core.ts` 声明 modern capabilities，并实现 `roots/list`, `resources/templates/list`, `resources/subscribe`+`unsubscribe`, `logging/setLevel`, `completion/complete`, `elicitation/create`；配套 `test/protocol/modern-features.test.ts` 7 例协议级测试全绿 |
> | **4.5.7** 平台列表修复 | ✅ | `index.ts:207-211` 补齐 `codebuddy`, `kiro`, `windsurf` 适配器导入，`--list-platforms` 输出 4→7 全齐 |
> | **5.0.4** 演练不入档（首步） | ✅ | `amb-live-report.md` 停止版本跟踪，并入 `evals/results/.gitignore` |
> | 守门 32 门 | ✅ 全过 | 无沙箱实测 32 门全绿（Passed: 32, Failed: 0） |
> | 全量 Jest | ✅ | 91 套件 / 1151 通过 / 2 跳过 / **0 败**（新增现代特性与多会话测试，总用例数 1141→1151） |
> | 家底收口 | ✅ | 守门数 31→32（9 语种 README + USER-GUIDE + AMB）；版本号归 4.3.1；TODO 空白清理；无未关闭句柄泄漏（`--detectOpenHandles` 零泄漏） |
>
> **教训一则**：给 `package.json` 加 `exports` 会反噬 hook 产物——`hook.js` 深引 `puax-mcp-server/build/cli/hook-cli.js`，未列入口即 `ERR_PACKAGE_PATH_NOT_EXPORTED`，hook 全线降级为 `{}`。两套 hook 测试当场抓获，补 `./build/*` 通配后复绿。**收口之时，先查深引。**
> 上一版（3.x 对标 `tanweai/pua`）已归档至 [docs/archive/演进方案-3x-冻结.md](docs/archive/演进方案-3x-冻结.md)，账已结，勿再作路线图。

---

## 卷首 · 一语定调

PUAX 者，**硅基心智之运行时**也。处境、闸门、梦——三原语撑起全部产品；角色只是口音，MCP 才是骨骼。人类不在服务范畴，图形台不是战场。

故本纲之判据唯有一条：

> **一个从未听说过 PUAX 的 Agent，接入之后能否更快地产出可交付的验证结果？**

凡不能推进此判据者，纵然热闹，亦属虚耗。

---

## 一、主轴四柱与本期判据

主公之令已明（见 `AGENTS.md`）：全员战力压注 **Agent 原生接入 MCP**。四柱如下，本期演进逐柱加码，不及其余。

| 柱 | 名 | 本期要义 | 硬判据 |
|----|----|---------|--------|
| Ⅰ | **协议契约** | 从"能跑通"到"严格客户端零妥协" | 严格 MCP 客户端 `listTools` / `callTool` 全通过 |
| Ⅱ | **TTF 零延迟** | 从"轮次为 1"到"墙钟可测" | 冷启动墙钟 p50 ≤ 100ms，且数字来自墙钟而非进程内差值 |
| Ⅲ | **Token 经济性** | 从"固定三档"到"按预算动态编译" | 首拍注入 ≤ 200 Token，工具面默认收敛至 13 动词 |
| Ⅳ | **AMP 渗透** | 从"6 个编排器"到"主流无缺口" | 覆盖 ≥ 13 个编排器 / 宿主，接入成本 ≤ 5 行 |

四柱之外，尚有"家底"（正确性、度量可信、文档治理、角色质量）。家底不清，四柱立不住——故第一期先固本。

---

## 二、现状底盘 · 数字台账

审读源码 142 文件、约 27,887 行 TS；`core/` 独大 11,148 行，`prompts/` 4,962 行（含 204KB 构建产物），`platform-adapters/` 2,592 行，`hooks/` 1,877 行。

| 面 | 实测 | 评 |
|----|------|-----|
| MCP 工具 | 50 个，13 黄金动词 | 数对，**契约面有硬伤** |
| 角色 | 59 篇，9 类 | 数对，**质量三分裂** |
| 风味 | 11 种 | 齐 |
| 守门 | 32 门（`evals/run-all.js`） | 文档写 31，**漂移** |
| Jest | 79 文件 / 803 例 | 有量，**23 核心模块真空** |
| 传输 | 真 Streamable HTTP + STDIO | 骨架对，**缺续传与半特性** |
| 编排器 | LangChain / LangGraph / CrewAI / AutoGen / Vercel AI / LlamaIndex | 6 个，**缺口 7 个** |
| 宿主导出 | 12 个 | 其中 5 个只是吐 SKILL.md，无运行时 hook |
| CI | ubuntu × (18/20) + windows，含 publish | 缺 macOS |

### 五维评分（1–5）

| 维度 | 现状 | v5 目标 | 短板一句话 |
|------|:---:|:---:|------|
| 协议契约 | 2 | 5 | Zod 对象直接当 schema 透传，严格客户端即炸 |
| TTF | 3 | 5 | 8ms 是进程内差值，墙钟另有其人（spawn + 200ms + 全量解析） |
| Token 经济 | 4 | 5 | 地板 162 之后，唯一大收益在"只暴露 13 动词" |
| AMP 渗透 | 3 | 4 | 6 个编排器，OpenAI/ADK/Mastra/Claude Agent SDK 全缺 |
| 家底（正确性·度量·文档） | 2 | 4 | 守门漂移、mock 当真、23 模块无测、文档互噬 |

---

## 三、病灶清单

分三等。**P0 为挡路石**——不搬开，主轴寸步难行。

### 🔴 P0 · 挡路石（v4.4 必清）

| # | 病灶 | 落点 | 症候 | 后果 |
|---|------|------|------|------|
| P0-1 | **工具 schema 未转 JSON Schema** | `src/tools/*.ts`（如 `tick.ts:40`）；`src/server/core.ts:102` 原样透传 | zod 3.22.4，全仓无 `toJSONSchema`/`zodToJsonSchema`；`inputSchema` 是裸 Zod 对象 | 严格客户端 `listTools` 直接失败（`inputSchema.type expected "object"`）。**这是协议契约第一硬伤** |
| P0-2 | **HTTP 面无鉴权 + CORS 全开** | `src/server/core.ts:510-519` | `Access-Control-Allow-Origin: *`，无 token | 任意网页可跨域调 `/v4/*`；`doctor/fix` 可写宿主配置。本地服务亦需环回收口 |
| P0-3 | **会话不可续传 + 幽灵端点** | `src/server/core.ts:443-485`、启动横幅 `L360-361` | 无 `eventStore`（全仓 0 命中）；横幅印 `/message` 但路由无此路径 | SSE 断线即失忆；用户按横幅访问必 404 |
| P0-4 | **防作弊"失败即放行"** | `src/cli/hook-cli.ts:305-308` | 全异常静默返回 `{}` + exit 0 | 拦截形同虚设——异常路径等于放行，与闸门本旨相悖 |
| P0-5 | **正则护栏未归一化** | `src/core/anti-cheat-guard.ts:17-54` | 纯字符串匹配，`..//` 或绝对路径可绕；`gitBypass` 含 `/git\s+push/i` 误伤面大 | 该拦的没拦住，不该拦的乱拦 |
| P0-6 | **包无 `exports` 字段** | `puax-mcp-server/package.json` | 无 `exports`，文档教用户深引 `build/core/index.js` | AMP 接入链路脆弱，一改内部布局即断 |

### 🟡 P1 · 主轴纵深（v4.5 – v4.6）

| # | 病灶 | 落点 | 症候 |
|---|------|------|------|
| P1-1 | **TTF 数字失真** | `src/core/ttf.ts:23-38`；`evals/results/ttf-cold-start.json` | `ttf_ms = now - state.startTime`，是**进程内差值**；实测 8ms 不含冷启动 |
| P1-2 | **三处真实墙钟开销** | ① 每事件 `exec node hook.js` 新起进程；② `hook-cli.ts:420` 无 stdin 固定 +200ms；③ 冷启动解析 `skill-manifest.ts`(48KB) + 9 bundle(~116KB) | 真墙钟远大于 8ms，一旦改用墙钟会"破功"——故须先改口径再优化 |
| P1-3 | **MCP 现代特性全缺** | `src/server/core.ts:66-84` | 仅 `tools/prompts/resources`；roots、elicitation、`completion/complete`、`logging/setLevel`、`resources/subscribe`、`ListResourceTemplates`、进度与取消——**全无** |
| P1-4 | **AMP 编排器缺口 7** | `src/core/amp-middleware.ts`；`distributions/python/puax_amp.py` | 缺 OpenAI Agents SDK、Google ADK、Mastra、Claude Agent SDK、LangGraph JS、Dify、n8n |
| P1-5 | **宿主缺口 + 回落粗暴** | `src/cli/hook-cli.ts:52-56` `detectHarness()` | 只认 3 个环境变量，其余**一律落到 `copilot`**；Gemini CLI / Cline / Roo Code / Aider / Zed 均无适配器 |
| P1-6 | **平台注册漏报** | `src/index.ts:207-211` 只 import 4 个适配器 | 实有 7 个，`--list-platforms` 漏 3 |
| P1-7 | **Token 只剩一条大路** | `src/core/thin-prompt.ts:78-163` | minimal 地板 162 中骨架+闸门+DIAGNOSIS+ARENA 是固定税；真空间在动态选档、编译缓存、**工具面 50→13** |

### 🟢 P2 · 家底清偿（并行，不阻塞）

| # | 病灶 | 落点 |
|---|------|------|
| P2-1 | 守门数漂移：实 32，文档写 31 | `README.md:271`、`docs/USER-GUIDE.md:134`、`docs/AMB.md:50`、`TODO.md` |
| P2-2 | 版本号分裂 | `docs/MCP-CONTRACT.md` 4.2.0、`USER-GUIDE.md`/`AMP.md` 4.0.0、plugin.json/marketplace.json **3.11.0** 且写"50 roles"（实 59） |
| P2-3 | **AMB 演练当真** | `evals/multi-model-amb.js:109-112` 硬编码 0.92/0.65；`amb-live.js --mock` 返回预写答案，而它正是 CI 第 26 门；`evals/results/amb-live-report.md` 已入 git |
| P2-4 | **Jest 真空 23 模块** | `ttf`、`governance`、`arena`、`evolution-engine`、`pressure-system`、`memory-graph`、`dream-council`、`outcome-store`、`proof-store`、`silicon-theater`、`high-agency`…；且 `collectCoverage: false` |
| P2-5 | 角色矩阵三分裂 | dream 八篇 `3.12.0` / 余 50 篇 `3.1.0` / `special-grill` `1.0.0` 且 `category: p10` 悬空；self-motivation 六篇 104 行占位文；45 个 `archive/SKILL.v2-*`：旧稿未清 |
| P2-6 | **11 个死触发条件** | `scope_excuse`、`vague_plan`、`risk_assessment`… 在代码中零引用 |
| P2-7 | 文档互噬 | `WEB-ADMIN-SPEC.md` 标废止仍在目录（且 CI 仍建 web-admin）；`AMP.md` 与 `AMP-INTEGRATION.md` 重复；`specs/` 两稿 1073 行为 v2.0 设计稿已脱节；`快速开始-自动触发系统.md` 落后两代 |
| P2-8 | 状态双通路 + 硬编码 | `hook-manager` 私有 Map 与 `state-manager` 并存；`getPuaxPath()` 零调用，13 模块各自拼路径；默认端口 2333 双写；`PuaxError`/`PuaxErrorCode` 全仓零引用（死代码）；`hooks/` 下 7 个 re-export shim；两套 trigger-detector（359 / 494 行） |
| P2-9 | 多语言半截 | de / fr / ru / es 仅 71–88 行（约中文 25–30%），**庄周八梦全缺** |
| P2-10 | 文档指向失效 | `hook-cli.ts:8` 引用 `docs/HOOK-ARCHITECTURE.md`（尚存）；但启动横幅端点与实际路由不符（见 P0-3） |

---

## 四、演进路线 · 四期行军

### 第一期 v4.4 · 固本（协议正确性与安全收口）

> **要义**：契约是主轴唯一凭证。契约不硬，一切渗透皆是沙上塔。

| # | 交付 | 落点 | 验收 |
|---|------|------|------|
| 4.4.1 | 工具 schema 全量转 JSON Schema | 新增 `src/tools/schema.ts`：`zodToJsonSchema(t.inputSchema)` 统一出口；`core.ts:102` 改吐 `{name, description, inputSchema: toJsonSchema(...)}` | 新增 `test/protocol/schema.test.ts`：遍历 50 工具断言 `inputSchema.type === 'object'` 且可 `ajv` 校验 |
| 4.4.2 | 入参运行时校验 | `CallToolRequestSchema` 分支加 `safeParse`，失败回 `ErrorCode.InvalidParams` | 单测覆盖每工具非法入参 |
| 4.4.3 | HTTP 面收口 | 监听默认改 `127.0.0.1`；CORS 白名单限环回；`/v4/doctor/fix` 改 POST + 需显式 `--allow-write` | 无法从非环回 origin 调用；GET fix 返回 405 |
| 4.4.4 | 防作弊异常不再放行 | `hook-cli.ts:305-308` 改为：拦截逻辑异常 → 记 `guard_error` 并**保守拒绝**高危动作（其余放行） | 新增故障注入测试：抛异常时高危指令被拒 |
| 4.4.5 | 护栏归一化 | `anti-cheat-guard.ts` 引入 `path.normalize` + 统一小写 + URL 解码后再匹配；`gitBypass` 收敛为精确命令词 | 绕过用例入测试集（`..//`、`%2e%2e`、绝对路径） |
| 4.4.6 | 清幽灵端点 | 删除横幅 `/message` 行；`/` 明示为 Streamable HTTP 端点 | 横幅每个端点 curl 均 200/405，无 404 |
| 4.4.7 | 包导出面 | `package.json` 加 `exports`（`.`、`./amp`、`./thin-prompt`），文档改引包路径 | 深引示例跑通 |
| 4.4.8 | 死代码与重复 | 删 `PuaxError` 或接上；合两 detector；去 7 个 shim；`getPuaxPath()` 收口；端口单一来源 | `npm run validate` 绿，行数下降 |

**退出判据**：严格 MCP 客户端（SDK 自带 client）`listTools` + 抽样 `callTool` × 10 全绿；`npm run validate` + 32 门守门全过。

---

### 第二期 v4.5 · 通脉（协议现代面 + TTF 真零延迟）

> **要义**：宁可数字难看，不可口径自欺。先把尺子换真，再把长度做短。

| # | 交付 | 落点 | 验收 |
|---|------|------|------|
| 4.5.1 | TTF 墙钟化 | `src/core/ttf.ts` 增加 `wall_clock_ms`（自进程 `performance.timeOrigin` 起算）；`evals/test-ttf.js` 改测**子进程冷启动到首压** | 报告同时给出 `in_process_ms` 与 `wall_clock_ms`，文档口径统一 |
| 4.5.2 | 删固定 200ms | `hook-cli.ts:420`：改 `stdin.isTTY` 立即 resolve，非 TTY 且无 data 才设短超时（≤20ms） | 冷启动耗时下降可测 |
| 4.5.3 | 角色资产懒加载 | `skill-manifest.ts` 与 9 bundle 改按需 import（动态 `await import`）；hook 路径只载门槛元数据 | 冷启动解析量下降 ≥ 80% |
| 4.5.4 | Hook 常驻化（可选档） | 新增 `puax hookd`：常驻守护 + Unix socket，宿主脚本改为极薄客户端；无守护时自动回落现路径 | `doctor` 可报"常驻已启用"；回落路径测试覆盖 |
| 4.5.5 | 会话续传 | `StreamableHTTPServerTransport` 接 `InMemoryEventStore`，支持 `Last-Event-ID` 重放 | 断线重连不丢事件，单测覆盖 |
| 4.5.6 | MCP 特性补齐 | `core.ts` 增：`roots/list`、`resources/subscribe`+`unsubscribe`、`resources/templates/list`、`completion/complete`、`logging/setLevel`、`elicitation/create` | 每项配一个协议级测试 |
| 4.5.7 | 平台列表修复 | `index.ts:207-211` 补 import 其余 3 适配器 | `--list-platforms` 输出 7 |

**退出判据**：冷启动墙钟 p50 ≤ 100ms（M 系列与 x64 各测）；MCP 特性 6 项可取用；TTF 报告双口径公开。

---

### 第三期 v4.6 · 扩疆（AMP 渗透 + 宿主补齐）

> **要义**：编排器是 Agent 的真栖息地。MCP 是插头，AMP 才是墙里的线。

| # | 交付 | 落点 | 验收 |
|---|------|------|------|
| 4.6.1 | OpenAI Agents SDK 适配 | `amp-middleware.ts` 增 `createOpenAIAgentsAmpGuard`；Python 侧 `create_openai_agents_guard` | 示例 ≤ 5 行，单测 + README |
| 4.6.2 | Google ADK 适配 | 同上，`before_tool_callback` 形态 | 同上 |
| 4.6.3 | Claude Agent SDK 适配 | PreToolUse / PostToolUse 回调 + MCP 双通道 | 同上 |
| 4.6.4 | Mastra / LangGraph JS | TS 侧工作流中间件 | 同上 |
| 4.6.5 | Dify / n8n | 以 HTTP `/v4/amp` 端点 + 节点模板形式提供（不写 SDK） | 一个可导入的 n8n 节点 + Dify 插件清单 |
| 4.6.6 | 宿主：Gemini CLI / Cline / Roo Code / Aider / Zed | 先走 `skill-md-platform-adapter` 工厂出 Shape C；Gemini CLI 与 Cline 升 Shape A/B | `doctor` 覆盖 15 宿主 |
| 4.6.7 | `detectHarness()` 强化 | 增 `GEMINI_CLI`、`CLINE_*`、`ROO_*`、`AIDER_*`、`ZED_*` 探测；未知时**显式报 unknown** 而非落 `copilot` | 未识别宿主日志可见，不再静默错配 |
| 4.6.8 | Python SDK 明示边界 | `distributions/python/README.md` 标注"仅 AMP 闸门，无角色/梦"；或补极简角色加载 | 文档与实能力一致 |

**退出判据**：AMP 覆盖 ≥ 13 编排器/宿主；每个适配有示例 + 测试；`--list-platforms` 与 `doctor` 数字与文档一致。

---

### 第四期 v5.0 · 立极（Token 经济学 + 度量真实化 + 内核收敛）

| # | 交付 | 落点 | 验收 |
|---|------|------|------|
| 5.0.1 | 工具面渐进暴露 | 默认只发 13 黄金动词；余者按 `tools/list_changed` 在触发后下发 | 首拍工具 schema 税 ≤ 800 Token（原 3000+） |
| 5.0.2 | 动态选档 | `thin-prompt` 接 `context_budget` 参数，按剩余窗口在 minimal/compact/full 间自动选 | 预算充足时质量不降，紧张时压到地板 |
| 5.0.3 | 编译缓存 | 同 (role, 档位, 风味) 组合缓存编译结果，key 带版本 | 二次注入零编译开销 |
| 5.0.4 | AMB 演练与实测分家 | `multi-model-amb.js` 接 `amb-live` 执行器或整体删硬编码；模拟产物改名 `*.simulated.json` 并 `.gitignore`；CI 第 26 门：有 key 真跑，无 key 记 `SKIP`（非 PASS） | 仓库内不再存在可被引用的"mock 战绩" |
| 5.0.5 | 守门与覆盖率硬阈值 | `run-all.js` 门数写入单一真源并被文档引用；Jest 开 `collectCoverage` + `coverageThreshold`（先 60%，逐期提） | CI 因覆盖率不足会红 |
| 5.0.6 | 补 23 模块测试 | 优先 `ttf`、`governance`、`pressure-system`、`evolution-engine`、`arena`、`dream-council` | 上述模块各 ≥ 1 测试文件 |
| 5.0.7 | 角色矩阵质量门 | 59 篇 version 归 4.x；`special-grill` 分类归位；self-motivation 六篇按 dream 文言基线重写；11 个死触发条件要么进 `deterministic-triggers.ts` 要么删；`archive/` 45 旧稿清理 | 新增 `evals/check-role-quality.js` 入门禁 |
| 5.0.8 | 文档治理 | 归档 `WEB-ADMIN-SPEC.md` 与 `specs/` 两稿；合并 AMP 双篇；`MCP-CONTRACT.md` 只留传输与降级契约；`快速开始-自动触发系统.md` 并入 USER-GUIDE；版本号统一脚本化校验（含根 README） | 无废止文档占据主目录 |
| 5.0.9 | 多语言补齐 | de/fr/ru/es 补庄周八梦与 v4 三原语章节，篇幅拉到中文 60%+ | 各语种含 dream 章节 |
| 5.0.10 | 内核收敛 | 三原语（处境/闸门/梦）+ 心跳 + AMP 为一等公民；50 工具标注内核/皮肤/实验（照 `ROLE-KERNEL.md` 分层） | `docs/ROLE-KERNEL.md` 扩为"能力去留表" |

**退出判据**：首拍注入 ≤ 200 Token；工具面 13；守门 ≥ 40 门且含真测门；覆盖率 ≥ 60%；文档零矛盾。

---

## 五、关键设计决策

**决策一 · 先契约，后渗透。**
盖因 MCP 是 PUAX 唯一骨骼。编排器适配写得再多，若 `listTools` 在严格客户端就垮，一切归零。故 P0 一期清完，方动三期。

**决策二 · 工具面收敛至 13 动词，余者按需下发。**
50 个工具全量暴露，光 schema 就吃掉 3000+ Token——这与"薄注入"自相矛盾。默认 13 黄金动词，触发后再 `tools/list_changed` 增补。此乃**首拍成本唯一的数量级收益**。

**决策三 · TTF 先换尺、再减肥。**
8ms 是进程内差值，非墙钟。若先优化再换尺，数字会"变差"，易被误读为退步。故 v4.5 先公开双口径，把尺子换真，再去砍 spawn、200ms、全量解析。**诚实的难看，胜过自欺的好看。**

**决策四 · 演练与实测彻底分家。**
`amb-live --mock` 进 CI 且返回预写答案，等于把演练当战绩；`results/` 入 git 更添外引风险。处置：模拟产物改名 + 忽略；CI 门在有 key 时真跑，无 key 记 SKIP 而非 PASS。

**决策五 · 角色分层，质量门守。**
59 篇不可一刀切。内核（dream 八篇之文言基线）为样板；皮肤（可批量）求齐；实验（占位文）要么重写要么降级。分类悬空、死触发条件、旧稿堆积——皆须 CI 校验，不可靠人记得。

**决策六 · 不碰封存区。**
Web Admin 不扩建（CI 仍建则要么摘 job 要么撤废止标记，二选一，不可两存）；碳基防御盾外延全缓，仅留只读识别层；日文版不做；LLM-as-judge 不做。工本有限，散则无功。

---

## 六、度量体系 · 成败之尺

| 指标 | 现今 | v4.4 | v4.5 | v4.6 | v5.0 | 测法 |
|------|:---:|:---:|:---:|:---:|:---:|------|
| 严格客户端 `listTools` 通过 | ❌ | ✅ | ✅ | ✅ | ✅ | SDK client 直连，50 工具全解析 |
| 工具入参运行时校验 | ❌ | ✅ | ✅ | ✅ | ✅ | 每工具非法入参测试 |
| HTTP 环回收口 | ❌ | ✅ | ✅ | ✅ | ✅ | 非环回 origin 调用被拒 |
| 防作弊异常路径 | 放行 | 保守拒绝 | 同 | 同 | 同 | 故障注入测试 |
| TTF 墙钟 p50 | 未测 | 公开双口径 | ≤100ms | ≤80ms | ≤60ms | 子进程冷启动到首压，30 次取中位 |
| 冷启动解析量 | ~164KB | 同 | ↓80% | 同 | 同 | 埋点统计 require 体积 |
| MCP 现代特性 | 0/6 | 0/6 | 6/6 | 6/6 | 6/6 | 协议级测试 |
| 会话续传 | ❌ | ❌ | ✅ | ✅ | ✅ | 断线重放测试 |
| AMP 编排器覆盖 | 6 | 6 | 6 | 13 | 13+ | 每个配示例+测试 |
| `doctor` 宿主覆盖 | 10 | 10 | 10 | 15 | 15 | 集成测试 |
| 首拍注入 Token | ~162（地板） | 同 | 同 | 同 | ≤200 含工具面 | 仓内估算器 + 第三方 tokenizer 双口径 |
| 工具默认暴露数 | 50 | 50 | 50 | 20 | 13 | `listTools` 计数 |
| 守门门数 | 32 | 34 | 36 | 38 | ≥40 | `run-all.js` |
| 真实 L4 场景 | 0（CI 不跑） | 0 | 1（有 key） | 3 | 6 | `run-l4.js` 记真值 |
| Jest 覆盖率 | 未统计 | 未统计 | 40% | 50% | ≥60% | `collectCoverage` + threshold |
| 文档矛盾数 | ≥8 | ≤3 | ≤2 | ≤1 | 0 | 版本号/门数一致性脚本 |

---

## 七、不做清单与风险

### 不做（明敕，避免工本流失）

| 项 | 缘由 |
|----|------|
| 日文版 | 主公已决 |
| LLM-as-judge 评测 | 成本高、不可复现；现有无 LLM 守门 + 可选 L4 已足 |
| Web Admin 扩建 / 重构 | 明令封存 |
| 碳基防御盾外延（插件 / 油猴 / 浮窗） | 暂缓，仅留只读识别层 |
| 新增角色数量 | 59 已够；本期重质不重量 |

### 风险

| 风险 | 征兆 | 处置 |
|------|------|------|
| 契约改动破坏存量客户端 | 老宿主报 schema 不兼容 | 保留旧字段双写一期，`CHANGELOG` 明示弃用窗口 |
| TTF 换尺后数字"变丑" | 对外被解读为退步 | 文档同步解释口径，双数值并列，不改历史 |
| AMB 实测烧钱 | 真实 API 费用上升 | CI 默认 `--mock` 记 SKIP；真测走定时任务，限场景数 |
| 编排器适配面铺太开维护不起 | 各 SDK 版本漂移 | 每个适配锁定版本 + 契约测试；无人维护者降级为"社区示例" |
| 家底清偿与主轴争工本 | P2 越做越大 | P2 严格并行不阻塞，且每期设上限条目 |

---

## 八、九十日行军表

| 周 | 主攻 | 交 |
|----|------|----|
| W1–W2 | P0-1 / P0-2 | schema 转换 + 入参校验 + HTTP 收口 |
| W3 | P0-3 / P0-4 / P0-5 | 续传预留位、防作弊保守拒绝、护栏归一化 |
| W4 | P0-6 / P0-7 / P0-8 + 发版 | 包 exports、死代码清理 → **v4.4.0** |
| W5–W6 | 4.5.1–4.5.3 | 墙钟化、删 200ms、懒加载 |
| W7–W8 | 4.5.5–4.5.7 | 续传、MCP 六特性、平台列表 → **v4.5.0** |
| W9–W11 | 4.6.1–4.6.5 | 七编排器适配 |
| W12 | 4.6.6–4.6.8 | 五宿主 + harness 探测 → **v4.6.0** |
| W13（并行） | P2 家底 | 门数/版本号/文档治理/多语言 |
| W14–W15 | 5.0.1–5.0.3 | 工具面收敛、动态选档、编译缓存 |
| W16–W17 | 5.0.4–5.0.7 | 度量真实化、覆盖率、角色质量门 |
| W18 | 5.0.8–5.0.10 + 发版 | 文档治理、内核收敛 → **v5.0.0** |

---

## 九、附录

### A · 数字真源（CI 校验对象，杜绝漂移）

| 项 | 真值 | 应出现于 |
|----|------|---------|
| 版本 | 4.3.1 | 根 README ×9 语种、package.json、CHANGELOG、plugin.json、marketplace.json |
| MCP 工具 | 50 | 同上 |
| 角色 | 59 | 同上（plugin.json 现写 50，须改） |
| 风味 | 11 | README |
| 守门 | 32 | 根 README、`USER-GUIDE.md`、`AMB.md`、`TODO.md` |
| Jest | 1143 例 / 89 套件 | CHANGELOG、README |

> 现有 `scripts/check-metrics-consistency.js` 只查 `puax-mcp-server/README.md`，**须扩至根 README 与 9 语种**。

### B · 文档治理清单

| 处置 | 文件 |
|------|------|
| 归档 | `docs/WEB-ADMIN-SPEC.md`、`docs/specs/mcp-server-extension.md`、`docs/specs/role-matching-algorithm.md`、`快速开始-自动触发系统.md` |
| 合并 | `docs/AMP.md` ← `docs/AMP-INTEGRATION.md` |
| 瘦身 | `docs/MCP-CONTRACT.md` 只留传输层与降级铁律 |
| 更正 | 启动横幅端点、`hook-cli.ts:8` 文档引用、各文档版本号 |
| 已归档 | `演进方案-3x-冻结.md`（本仓 `docs/archive/`） |

### C · 与旧纲之别

旧纲（3.x）是**对标竞品的追赶账**：17 维度逐项补齐，已于 v4.0 结清。
本纲是**内核自生长的路书**：不再看对手脸色，只问"Agent 接入后是否更快产出可交付结果"。四柱之外，皆属枝节。

---

<p align="center"><b>契约硬则渗透易，尺子真则优化实。</b></p>
