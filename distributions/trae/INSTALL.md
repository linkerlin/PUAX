# Trae 平台安装

```bash
npx puax-mcp-server --export=trae --output=./puax-export/trae
```

将 `skills/` 目录复制到 Trae agent skills 路径。

英文版：

```bash
npx puax-mcp-server --export=trae --output=./puax-export/trae --lang=en
```
